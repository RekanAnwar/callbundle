name: callbundle_platform_interface
description: "Platform interface for the CallBundle federated Flutter plugin. Defines the abstract API contract, data models, and events for native call UI."
version: 1.1.0
homepage: https://ikolvi.com
repository: https://github.com/Ikolvi/callbundle/tree/main/callbundle_platform_interface
issue_tracker: https://github.com/Ikolvi/callbundle/issues

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
